{% extends 'layout.twig' %}

{% block body %}
  <h1>{{title}}</h1>
  <div id="posts">
      <!--<div class="postit">Coucou</div>
      <div class="postit">Hello</div>
      <div class="postit">Je suis un post-it de qualitey</div>
      <div class="postit">Coucou</div>
      <div class="postit">Hello</div>
      <div class="postit">Je suis un post-it de qualitey</div>-->
  </div>
    
    <div id="acces_code">
        <img src="/qr" alt="">
    </div>

  <script src="/javascripts/socket.io.js"></script>
  <script>
      var socket = io.connect('http://localhost:3000');
      socket.on('news', function (data) {
          console.log(data);
          socket.emit('my other event', { my: 'data' });
      });
      socket.on('postit', function(data){
          console.log(data);
          document.getElementById('posts').innerHTML += '<div class="postit">'+data.content+'</div>';
      });
  </script>
{% endblock %}
